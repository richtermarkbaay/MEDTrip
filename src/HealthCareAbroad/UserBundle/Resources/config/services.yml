parameters:
    user_service_class: HealthCareAbroad\UserBundle\Services\UserService

services:
    user_service:
        class: %user_service_class%
        arguments: [@chromedia_global_request, @?doctrine]
