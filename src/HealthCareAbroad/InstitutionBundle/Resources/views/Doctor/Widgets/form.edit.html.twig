<form method="post" id="edit-doctor-form" class="edit-doctor-form" {{ form_enctype(editDoctorForm) }} style="display:none">
    <div class="row-fluid">
        <div class="control-group span6">
            {{ form_row(editDoctorForm.lastName, { 'attr': {'class': 'span12'} }) }}
        </div>
        <div class="control-group span6">
            {{ form_row(editDoctorForm.firstName, { 'attr': {'class': 'span12'} }) }}
        </div>
    </div>
    <div class="row-fluid">
        <div class="control-group span6">
            {{ form_row(editDoctorForm.middleName, { 'attr': {'class': 'span12'} }) }}
        </div>
        <div class="control-group span6">
            {{ form_row(editDoctorForm.suffix, { 'attr': {'class': 'span12'} }) }}
        </div>
    </div>
    <div class="row-fluid">
        <div class="control-group">
            <label for="specializations">Doctor's Specializations</label>
            {{ form_widget(editDoctorForm.specializations, {attr: {class: 'span12'} }) }}
        </div>
    </div>

    <div class="row-fluid">
        <div class="control-group span12">
            <label for="Email Address">Doctor's Email Address</label>
            {{ form_widget(editDoctorForm.contactEmail, { 'attr': {'class': 'span6'} }) }}
            <label><small><em>Note: This email will not be listed on the webpage.</em></small></label>
        </div>
    </div>
    
    <div class="row-fluid">        
        {% for widget in editDoctorForm.contactDetails %}
            {{ form_widget(widget) }}
        {% endfor %}
    </div>
    <label style="margin-left: 10px; margin-bottom: 10px;">
        <em><small>Note: This number will not be listed on the webpage.</small></em>
    </label>

    {{ form_widget(editDoctorForm._token) }}
</form>