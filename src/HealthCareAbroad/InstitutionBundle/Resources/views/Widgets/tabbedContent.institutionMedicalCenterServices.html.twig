{% embed 'InstitutionBundle:Widgets:tabbedContent.base.html.twig' %}
    {% block tabbedContent %}
	    
			<div class="maincontentblock">
			    <table class="table table-striped table-bordered bootstrap-datatable">
			      <thead>
                    <tr>
        				<th>Services</th>
        				<th>Actions</th>
        			</tr>
                </thead>
			        <tbody>
		             {% for service in services %}
        				<tr>
        					<td>{{ service.name |title}}</td>
        					<td>
        						<div class="post-toolbar">
        							<i class="icon-trash" title="Delete"></i>
        							<a href="#deletetreatment" title="" role="button" data-toggle="modal">Delete</a>
        						</div>
        					</td>
        				</tr>
        		      {% else %}
            		  	<tr>
            			   <td> No Services yet. </td>
            			   <td></td>
            			</tr>
        			{% endfor %}
        			</tbody>
    			</table>
            </div>
                
    {% endblock %}
{% endembed %}