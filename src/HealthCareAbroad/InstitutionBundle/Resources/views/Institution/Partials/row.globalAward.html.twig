<tr id="award{{ award.id }}">
	<td><h5 class="award_name">{{ award.name }}</h5><small class="awarding_body_name"> {{ award.awardingBody.name }} </small></td>
	<td>
	 <div class="post-toolbar treatment_action">
    		<i class="icon-trash" title="Delete"></i>
    		<a data-toggle="modal" data-id="{{ award.id }}" data-target="#institutionGlobalAwardModal"  onclick="InstitutionProfile.showCommonModalId(this); return false;" href="#">
            <small>Delete</small></a>
    	</div>
	</td>
</tr>