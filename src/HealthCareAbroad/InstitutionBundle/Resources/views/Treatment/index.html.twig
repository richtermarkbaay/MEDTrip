{% extends 'InstitutionBundle::layout.html.twig' %}
{% block title %}{{ parent() }} Treatments {% endblock %}
{% set selectedTab = 'medical_centers'%}

{% block content %}
<!-- start content -->
<div id="content">
    <!--  start page-heading -->
    <div id="page-heading">
    	<h1>{{ app.session.get('institutionName') }} Treatments</h1>
    </div>

    <!-- end page-heading -->
    
    {% embed '::contentWrapper.admin.html.twig' %}
        
        {% block wrappedContent %}
            
            {# ----- Related tasks ----- #}
            {% include 'InstitutionBundle:Treatment:relatedTasks.html.twig' with {hideTreatmentManagement: true} %}
            {# ----- end Related tasks ----- #}
            
			<!--  start table-content  -->
			<div id="table-content">
			    {% include '::notice.admin.html.twig' %}
			    
			    <div id="institution-medical-procedure-types" style="width:75%">
			        {% include 'InstitutionBundle:Treatment:treatments.html.twig' with {'institutionTreatments': institutionTreatments} %}
			    </div>
                <!-- end institution-medical-procedure-types  -->            			    
				
			</div>
			<!--  end table-content  -->
        {% endblock %}
        
    {% endembed %}

</div>
<!--  end content -->
<div class="clear">&nbsp;</div>
{% endblock %}

{% block inlineJavascriptCode %}
	<script src="{{ asset('bundles/institution/js/InstitutionTreatmentProcedure.js') }}"></script>
	<script type="text/javascript">
		$(function(){
			$("#dialog-container").dialog({
				title: 'Treatment Procedure',
				autoOpen: false,
				width: 700,
				modal: true,
				resizable: false,
				//buttons: {"Save": function() {var bValid = true;}, Cancel: function() {$( this ).dialog( "close" );} },
				close: function() {}
			});

			$("a.add-edit-procedure").click(function() {
				var elem = $(this);
				var _url = elem.attr('href');

				$("#dialog-container").empty();
				$("#dialog-container").dialog({
					open: function() {
						$.ajax(_url)
							.done(function (data) {
								$('#dialog-container').html(data);
							});
					},
				});
				$("#dialog-container").dialog("open");
				return false;
			});

			{% if institutionTreatments is not empty %} 
		    $("#institution-medical-procedure-types").accordion();
		    {% endif %}			
		});
	</script>
{% endblock %}
