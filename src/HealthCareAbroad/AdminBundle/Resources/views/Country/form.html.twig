{% extends 'AdminBundle::layout.html.twig' %}
{% set selectedTab = 'manageHcaData' %}
{% set selectedSubTab = 'manage_countries' %}

{% block content %}

	<div id="content">
		<div id="page-heading"><h2>{{ id ? 'Edit' : 'Add' }} Country</h2></div>

		{% embed '::contentWrapper.admin.html.twig' %}
			{% block wrappedContent %}

            {% set contents = [{
					'title': 'View all countries', 
					'description': 'View the list of countries',
					'href': path('admin_country_index'), 
					'links': [{'title': 'View countries', 'href': path('admin_country_index')}]
				}]
            %}

			{% include '::relatedTasks.base.admin.html.twig' with {contents: contents} %}
			
			<form novalidate="novalidate" action="{{ formAction }}" method="post" {{ form_enctype(form) }} class="basic-form">

				{{ form_row(form.name) }}
				{{ form_row(form.status) }}
				{{ form_rest(form) }}

			    <input type="submit" class="form-submit"/>
			</form>
			{% endblock %}
		{% endembed %}
	</div>

{% endblock %}