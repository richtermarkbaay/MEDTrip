{% embed 'FrontendBundle:Embed:modalBox.base.html.twig' with {'modal': {'id' : 'report', 'title': 'Submit Report', 'formId': 'errorReport'}} %}
    {% block modalBody %}
    <form action="{{ path('frontend_page_exception_report_send') }}" method="POST" {{ form_enctype(form) }} class="basic-form" id="errorReportForm">
    	<div class="alert-box"></div>   
        <div class="details control-group row-fluid">
            <label for="{{ form.children.details.vars.id }}" >
                <strong>{{ form.children.details.vars.label }}</strong>
            </label>
           {{ form_widget(form.details, { 'attr': {class: 'span12', cols: 30, rows: 10} })  }}
        </div>
		 <div class="captcha control-group row-fluid">
		     <label for="{{ form.children.captcha.vars.id }}" >
                <strong>{{ form.children.captcha.vars.label }}</strong>
            </label>
             {{ form_widget(form.captcha) }}
        </div>
			
		{{ form_rest(form) }}
	</form>
   {% endblock %}
   {% block footerBody %}
        <button class="btn" data-dismiss="modal" aria-hidden="true"> Cancel</button>
        <button data-loader-text="Sending..." class="btn orange" id="errorSubmit">Send Now</button>
    {% endblock %}
{% endembed %}


