<section class="widget-wrapper">
    {% if title is defined and title|length %}
	    <h4 class="widget-heading {{ headingClass is defined ? headingClass : ''}}">{{ title | raw }}</h4>
	{% endif %}
	{% block asideContent %}{% endblock %}
</section>