parameters:
    memcache_namespaces:
        #-- start InstitutionBundle
        #-
        
        # namespace in context of a specific institution
        institution.single_entity.base: 
            pattern: institution_{institutionId}
            separator: ~